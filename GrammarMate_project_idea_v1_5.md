# GrammarMate — Project File (единый документ идеи)
Версия: 1.3  
Дата: 2026-01-08  

Этот документ — единая спецификация идеи GrammarMate/Deep Grammar Trainer: методика, структура контента, режимы, бизнес‑правила и минимальный набор экранных форм (Home / Settings / Training / Story Quiz).  
Он объединяет исходный документ (v1.2 от 2026‑01‑05) и решения из обсуждения по UX/экранам.

---

## 0) Ключевые ограничения реализации (MVP)
1) **Без сервера и без базы данных** (ни SQL, ни NoSQL).  
2) Персистентность — **файлы** (YAML/JSON/CSV) + атомарная запись (temp → fsync → rename).  
3) Контент загружается пакетами (**Lesson Packs**) через Settings.  
4) UI — **stateless renderer**: отображает состояние, расчёты делает домен/Use Cases.  
5) Все схемы файлов имеют `schemaVersion` + миграции.

---

## 1) Миссия и цель
**GrammarMate** — тренажёр, который формирует **автоматизм грамматических паттернов** в переводе **RU → Target** (на старте: English/Italian).  
Цель не “знать правила”, а **быстро и без размышлений** выбрать корректную конструкцию и сказать/ввести перевод.

Ключевая метрика успеха пользователя:
- “Вижу русскую мысль → мгновенно кодирую её в целевом языке нужным паттерном”.

---

## 2) Основные принципы обучения (что делает систему эффективной)
Система объединяет лучшие практики формирования навыка:

1) **Retrieval practice**: ответ не показывается сразу.  
2) **Error-based learning**: ошибки — обучающий сигнал (но для STT “верно/неверно” не единственный сигнал).  
3) **Chunking**: обучение дробится на малые блоки (подуроки) по 8–15 предложений.  
4) **New-only → Mixed**: сначала чистый ввод паттерна, затем дозированное смешивание для тренировки выбора паттерна среди конкурентов.  
5) **Spaced repetition без календаря**: интервалы задаются **в шагах программы** (по количеству пройденных подуроков), а не днями/часами.  
6) **Низкий порог старта**: Warm-up на 3 лёгких предложения перед подуроком (быстрый “вход” в работу).  
7) **Опциональные “ударные сессии”**: Boss-челленджи для проверки устойчивости автоматизма.  
8) **Поддержание после курса**: бесконечный “Elite Mix” режим для фонового удержания навыков.

---

## 3) Контент и структура курса

### 3.1 Темы
- **12 грамматических тем (уроков)** — каркас курса.
- В каждой теме ~**100 предложений** (целевое значение; допустим диапазон 50–150 в зависимости от темы).

Примечание: желательно придерживаться правила **“1 тема = 1 паттерн”** (не смешивать в уроке конструкции, которые требуют другого времени/модальности), чтобы не разрушать моно-обучение.

### 3.2 Подуроки (кружочки как в Duolingo)
Каждая тема разбивается на **10–12 подуроков** (“кружочки”).  
**Размер подурока:** 8–15 предложений (рекомендуемый дефолт: **10**).

Итого в программе:
- 12 тем × (10–12 подуроков) = **120–144 подурока**.

### 3.3 Active Set / Reserve Set (150/150) — рост сложности без разрушения паттерна
Чтобы пользователь не “заучивал конкретные фразы”, а автоматизировал **паттерн**, у каждого урока (темы) вводится два пула предложений:

- **Active Set** — основной набор для первичного обучения темы (по умолчанию **150 предложений**).
- **Reserve Set** — скрытый резерв вариативности (ещё **150+ предложений**), который **не показывается в Lesson N на этапе New-only**, но подмешивается позже.

Типовой объём темы: до **300 предложений** (150 Active + 150 Reserve).  
Допускается расширение Reserve (например, 150→300+) без изменения структуры урока.

Правила использования:
1) **New-only подуроки темы N**: только **Active Set темы N** (чистый ввод паттерна).
2) **Mixed подуроки темы N**: текущая тема (Active) + повторы прошлых тем; при этом повторы берутся **в приоритете из Reserve прошлых тем**.
3) **Начиная с урока N+1**: Reserve темы N становится основным источником повторов (чтобы повтор был “новым в старом”).
4) **Boss / Elite**: могут использовать Active+Reserve (см. разделы 7 и 9).

Разблокировка Reserve (дефолт):
- Reserve темы N становится доступен для подмешивания после завершения **Program-ветки** темы N (все кружочки),  
  либо (более жёстко) после завершения темы **N+1** — чтобы гарантировать “задержку” и усилить эффект переноса.

### 3.4 Онлайн-обновления контента (опционально)
Если появится сервер, контент можно “подливать” без обновления приложения:
- добавлять новые **Reserve-пакеты** к уже существующим темам (фоновые апдейты сложности),
- расширять Elite Mix новыми предложениями, сохраняя “1 тема = 1 паттерн”.

Ограничения качества (обязательны):
- каждый пакет строго привязан к `lesson_id` и `pattern_tag`,
- пакет не должен приносить конструкции из других тем,
- версии пакетов повторяемые (детерминированная выборка), чтобы не было хаоса прогресса.

---

## 4) Режимы тренировки

### 4.1 Program / Path (основной режим)
Основной режим обучающей программы.

### 4.2 Boss-челленджи (опциональные “ударные” задания)
См. раздел 7.

### 4.3 Elite Mode (после 12-го урока)
См. раздел 9.

---

## 5) Program Mode — как устроено обучение внутри 12 тем

### 5.1 Типы подуроков внутри одной темы
Чтобы не “мешать” ввод новой темы:

- **New-only подуроки**: только текущая тема, **только Active Set** (без вставок старых тем).
- **Mixed подуроки**: текущая тема (Active) + дозированный повтор прошлых тем; источник повтора — **в приоритете Reserve** прошлых тем.

### 5.2 Обновлённая фазность New-only → Mixed (фиксируем)
Уточнение из обсуждения:

- **Первые 3–4 подурока каждой темы** — **строго New-only** (только новая тема).
- Начиная с подуроков **5+** — допускается Mixed.

> Конкретные правила миксирования, выбор “какие прошлые темы”, доли, слоты и детерминированная генерация пулов делаются отдельным документом: **Mixing Scheduler BR**.  
> В этом документе фиксируется принцип фазности и совместимость с общей лестницей repetition.

### 5.3 Warm-up (допамин + вход в работу)
Перед каждым подуроком (или перед началом сессии) добавляется **Warm-up mini-block**:
- **3 очень лёгких предложения**
- цель: снизить барьер старта, дать быстрый успех и “включить двигатель”
- Warm-up **не считается** прогрессом освоения темы (не повышает “уровень кружочка”), это ритуал входа.

Правила:
- Warm-up берётся из “easy pool” (высокая вероятность успеха).
- Набор Warm-up должен меняться (не одни и те же 3 предложения каждый раз).
- Warm-up **не влияет** на ChunkStrength/Freshness.

### 5.4 Подсказки (Hint): вместо “3 ошибки” — контроль долей
В MVP нам **не критично** правило “после 3 ошибок показываем ответ”; критична **доля подсказок** как индикатор “не было retrieval”.
При этом механика показа правильного ответа фиксируется как UX‑поведение и **не считается подсказкой** для метрик (из‑за ошибок STT).

Правила:
- Для подурока считаем `hintUsageRate = hintedItems / totalItems`.
- Если `hintUsageRate > 40%` → урок не может перейти в FLOWER (остаётся SPROUT).
- Если `hintUsageRate > 60%` → “закрепление” блокируется: FLOWER недоступен, пока показатель не снизится дальнейшей практикой (меньше подсказок).

Дополнительно:
- Кнопка “показать ответ” доступна всегда (иконка), показывает правильный ответ и переводит сессию в режим ожидания (авто‑STT выключен, таймер на паузе).
- После 3 ошибок также показывается правильный ответ; авто‑STT останавливается, сессия ждёт действия пользователя.
- Показ ответа **не закрывает** карточку и не пропускает шаг, пользователь может продолжить работу с текущим предложением.

При этом:
- Подурок можно завершить и идти дальше по программе; мы не “наказываем” остановкой, мы блокируем **признание автоматизма**.

---

## 6) Spaced repetition в шагах (не в днях)

### 6.1 Почему так
Пользователь может заниматься нерегулярно, поэтому календарные интервалы (1 день, 2 дня…) плохо контролируются.  
Вместо этого используем интервалы в **количестве пройденных подуроков**.

### 6.2 Лестница интервальных повторов (в шагах подуроков)
Для повторения старых тем в Mixed-подуроках используется “плотная” лестница:

**+1, +2, +4, +7, +10, +14, +20, +28, +42, +56**

Где “+k” означает: вставить повтор после того, как пользователь прошёл ещё k подуроков программы.

### 6.3 Ограничения (чтобы не раздувать подурок)
- Повторы вставляются **только** в Mixed-подуроки.
- В одном Mixed-подуроке выделяется фиксированное число “review slots”:
  - стандарт: **20%** (например, 2 из 10 предложений)
  - для последних тем (10–12): **30–40%** (например, 3–4 из 10), чтобы добить поздние темы по повторениям
- В одном подуроке желательно не давать более **1 предложения** от одной старой темы.
- В review-слотах приоритет источника: **Reserve прошлых тем → Active прошлых тем**.

### 6.4 Выбор старых тем для вставок
Приоритет:
1) тема N-1
2) темы N-2..N-4
3) более старые темы — по “due” из лестницы и/или по фрикции

---

## 7) Boss-челленджи (опциональные “ударные” задания)

### 7.1 Final Boss для каждого урока
Под каждым уроком есть отдельная ветка/узел: **Final Boss** (опционально).  
Правило:
- пройти **все предложения** текущего урока **за одну сессию** (без ограничения по времени)
- медали:
  - **Bronze**: ≥ 30%
  - **Silver**: ≥ 60–70% (фиксируется продуктом)
  - **Gold**: 100%

Пул предложений в Boss:
- Bronze/Silver: только **Active Set** урока.
- Gold: **Active + Reserve**.

### 7.2 Super Boss прогресса (накопительный)
При переходе к уроку N+1 предлагается опциональный Super Boss:
- пройти микс **всех предложений** уроков 1..N (одна сессия)
- медали по тем же правилам

Цель Boss:
- стресс-тест устойчивости под интерференцией
- проверка “длинной дистанции” (endurance)

### 7.3 Boss Quest (опционально)
Опционально, после прохождения Final Boss или Super Boss:
- открыть **Boss Quest**: короткая история (или микро-сцены) с высокой интерференцией
- затем 10–15 вопросов на понимание
- цель: проверить не только “могу построить форму”, но и “понимаю смысл в потоке”

### 7.4 Продуктовая подача Boss/Mix (фиксируем)
Boss-режимы не обязаны быть отдельными “в середине урока”.  
В MVP: **после полного завершения урока** показываем опциональный выбор:
- Final Boss (текущий урок)
- Super Boss / Mix (по продуктовым условиям)
- Пропустить и идти дальше

---

## 8) Голосовой ввод (Google STT) и метрики
STT распознаёт с ошибками, поэтому **нельзя жёстко доверять “верно/неверно”** как единственному сигналу прогресса.

### 8.1 Метрики “фрикции”, устойчивые к STT
- **Cards per session**
- **Cards per active minute**
- **Attempts per card**
- **Hint rate**

STT‑поведение (MVP):
- распознанный текст **сразу отправляется на проверку** (auto‑submit), без обязательного подтверждения кнопкой.
- после показа правильного ответа авто‑запуск STT отключается до действия пользователя.

### 8.2 Отметка “STT ошибся” (рекомендуемо)
После подсказки/серии ошибок:
- “Я сказал правильно, распозналось неверно”
- “Я ошибся”

---

## 9) Elite Mode после 12-го урока (фоновая поддержка)

### 9.1 Недельный структурный цикл (7 шагов, без календаря)
В Elite Mode есть **7 шагов цикла** (Step 1..Step 7).  
Пользователь занимается когда хочет; приложение выдаёт следующий шаг цикла: 1→2→…→7→1→…

### 9.2 Разбиение каждого урока на 7 пакетов
Каждый урок формирует общий пул (Active+Reserve; по умолчанию 150/150). Для Elite Mode этот пул разбивается на **7 пакетов** по ~14–15 предложений:
- Lesson i: P1..P7 (перемешанные группы)
- пакеты можно перетасовывать периодически (например, раз в 2–4 недели циклов)

### 9.3 Состав одного Elite-подурока (пример на 30 предложений)
Рекомендуемый состав пула в Elite Mode:
- 70% Active / 30% Reserve (по умолчанию)
- со временем можно смещать к 50/50

- Warm-up: 3 лёгких предложения
- Focus: 21 предложение (3 темы × 7 предложений из их текущего пакета Pk)
- Noise: 6 предложений (по 1 предложению из 6 других тем)

Итого: 30 предложений.

### 9.4 Ротация “тем-фокусов”
Пример:
- Step 1: темы 1–3
- Step 2: 4–6
- Step 3: 7–9
- Step 4: 10–12
- Step 5: “слабые 3” (по фрикции)
- Step 6: “случайные 3”
- Step 7: **Weekly Boss** (опционально)

### 9.5 Weekly Boss (опционально)
На Step 7 предлагается:
- 60–120 предложений, микс всех 12 тем
- медали bronze/silver/gold по % пройденного за одну сессию

---

## 10) Нормализация ответов (кратко)
Перед сравнением:
- trim + схлопывание пробелов
- lower-case
- игнор части пунктуации
- **апостроф внутри слова сохраняется** (don’t ≠ dont)
- допускаются альтернативы (do not / don’t и т.п.)

UI‑правило:
- альтернативы могут использоваться для проверки, но **в интерфейсе не показываются**.

---

## 11) Примечание: Listening / Reading / Vocabulary — отдельные типы уроков
Слух, чтение и новые слова — **отдельные направления**, делаются независимо от “кружочков грамматики”. 
### 11.1 Listening (аудирование) - этого вида уроков нет у нас.
- цель: распознавание речи и понимание на слух
- формат: короткие фрагменты + простые вопросы/выбор ответа 

### 11.2 Reading (чтение) - это story in - out
- цель: скорость понимания текста и устойчивость к “живым” формулировкам
- формат: текст + вопросы на понимание

### 11.3 Vocabulary (новые слова) - слова карточки
- цель: расширение словаря отдельными карточками/мини-пакетами
- формат: распознавание/соотнесение, короткие примеры

### 11.4 Story Check-in / Story Check-out вокруг каждого грамматического урока
Для **каждой темы N** в Program Mode существуют два связанных мини‑урока:

#### A) Story Check-in (до начала урока N) — диагностика
1) Пользователь читает короткую историю (без перевода), где высокая доля предложений использует целевой паттерн темы N.
2) Затем проходит квиз 10–15 вопросов на понимание (Multiple Choice / True-False).
3) Система фиксирует метрики “до”:
   - accuracy
   - time-to-answer
   - hint rate (если есть подсказки)

#### B) Story Check-out (после завершения урока N) — оценка прогресса
1) Пользователь читает вторую историю по той же теме N (не та же самая).
2) Проходит квиз 10–15 вопросов.
3) Система показывает сравнение:
   - “до”: X/Y, “после”: A/B
   - изменение скорости
   - подсказки/фрикция

Рекомендация по контенту:
- Story Check-in может использовать более “мягкую” лексику/длину.
- Story Check-out должен включать больше вариативности (в т.ч. Reserve, если он уже разблокирован).

Важно:
- Истории — это **не замена** RU→Target retrieval-тренинга.
- Истории не смешиваются внутрь New-only подуроков грамматики.

---

## 12) Межурочные блоки “Vocabulary Sprint” (active recall)
Цель: поддержать лексику, необходимую для построения предложений, **не ломая** грамматический retrieval‑тренинг, и тренировать **активное извлечение**, а не узнавание.

### 12.1 Принцип отделения
Vocabulary Sprint — отдельный тип упражнений, который:
- не считается “кружочком грамматики”,
- может выполняться быстрее, чем перевод предложений,
- не должен смешиваться внутрь New-only подуроков грамматики, идет как 3-5 отдельных подурока блока урока.

### 12.2 Два типа заданий (оба — active recall)
**Тип A: RU → Target**
- русское слово/фраза слева
- ответ замылен / поле ввода / микрофон
- до 3 попыток, затем показывается правильный ответ(ы)

**Тип B: Target → RU** - нет такого у нас. Только native -> target
- слово на целевом языке слева
- русский ответ замылен
- до 3 попыток, затем подсказка

### 12.3 Формат пакета между уроками
- 1 блок = **6 слов**
- Между уроками: **3 блока × 6 слов = 18 слов**
- микс: 2 блока типа A + 1 блок типа B

### 12.4 Правила ответов (как для предложений, но проще)
- варианты (синонимы/формы) через `+`
- нормализация: trim, lower-case, схлоп пробелов, простая пунктуация игнорируется
- апостроф значим (don’t ≠ dont)

### 12.5 Как выбирать слова
решает методолог. Слова будут в отдельном словаре. учти это в модели домена. к каждому слову нужно сделать флаг легко, сложно. легко убирает слово, сложно увеличивает его подмешивание и формируется список тяжелых слов для отдельного вида упражнения - повтор слов.

### 12.6 Спейсинг слов “через 1–2 урока” (без календаря)
- После Lesson N: 3 блока (18 слов) — “новые”
- Перед Lesson N+1: 1 блок (6 слов) — повтор
- Перед Lesson N+2: 1 блок (6 слов) — повтор



---

## 13) Итоговый порядок “вокруг урока” (флоу)
Путь пользователя по каждой теме N:

1) **Story Check-in (до урока N)**  (можно пропустить нажав "пропустить")
2) **Program Lesson N (подуроки грамматики)**  
   - Warm-up (3 лёгких предложения)  
   **Vocabulary Sprint пакет Lesson N**  - 1 подход
   - первые 3–4 подурока New-only  
   **Vocabulary Sprint пакет Lesson N**  - след подход
   - далее Mixed по шедулеру  
3) **Final Boss Lesson N (опционально)**  
4) **Vocabulary Sprint пакет Lesson N**  
5) **Story Check-out (после урока N)**  
6) **Boss Quest (опционально)**  
7) Переход к Lesson N+1  
   - перед Lesson N+1 и N+2: короткий повтор Vocabulary Sprint N

   Учесть в бизнес правилах - при каждом успешном завершении урока (учитывая хинты) ставим прогресс навыка.

---

## 14) UI: Home / Settings / Training (экранные формы как часть логики)

### 14.1 Home Screen / Entry Point
Home — точка входа в методику.

Зоны:
- Identity & Context: профиль, язык, ⚙ Settings
- Primary action: одна кнопка (Start / Continue / Resume)
- Grammar Skill Matrix 4×3 (12 уроков) с состояниями
- Legend
- Method button (“How This Training Works”)

Состояния урока (domain):
```kotlin
enum class LessonState { SEED, SPROUT, FLOWER, LOCKED }
```

Presentation overlay:
- `FLOWER + low freshness` → 🕸 overlay (паутинка)

Use cases:
- UC-H-01 первый запуск
- UC-H-02 возврат пользователя
- UC-H-03 смена языка
- UC-H-04 тап по LessonTile (vNext: детали урока)
- UC-H-05 переход в Settings

### 14.2 Settings Screen
Назначение: управление контентом и средой (не “методика в моменте”).

Разделы:
- Profile & Portability (export/import profile)
- Learning language (дубль)
- Lesson packs (import/delete/batch)
- Input & Audio (sound, voice, STT‑параметры)
- About/Diagnostics

Use cases:
- UC-S-01 Import Lesson Pack
- UC-S-02 Import Batch
- UC-S-03 Delete Pack
- UC-S-04 Delete All Lessons
- UC-S-05 Reset Progress
- UC-S-06 Export Profile
- UC-S-07 Import Profile
- UC-S-08 Toggle sound/voice

### 14.3 Training Screen (игровое поле перевода)
Ключевое UX‑решение: пользователь **не видит** “Block X/Y” и **не видит** количество предложений; видит только прогресс в процентах и темп.

Элементы:
- RU prompt
- поле ввода EN (text/voice)
- progress bar (процент)
- Tempo: sentences per minute
- ⏮ Prev / ⏯ Play‑Pause / ⏭ Next
- 💡 Answer (иконка) — показать правильный ответ; назначение показывается всплывающей подсказкой
- 🔊 Speak (TTS) в режиме показа ответа (offline, если доступно)
- Exit

Use cases:
- UC-T-01 Start/Pause
- UC-T-02 Input answer
- UC-T-03 Check
- UC-T-04 Continue/Next item
- UC-T-05 Prev/Next navigation
- UC-T-06 Answer + Speak
- UC-T-07 Exit

BR:
- alt не показываем
- Warm-up обязателен и не влияет на метрики
- hintUsageRate влияет на FLOWER (40/60)
- повторная проверка уже закрытого item не увеличивает прогресс навыка
- Tempo — UX‑метрика (cards per active minute)

### 14.4 Story Quiz Screen (Check-in/Check-out)
Формат: текст истории + серия вопросов (MCQ/TF).  
Ответы выбираются, не требуется пересказ/написание.

Use cases:
- UC-Q-01 Start story
- UC-Q-02 Answer question
- UC-Q-03 Finish quiz → сохранить метрики “до/после”

---

## 15) Контент: Lesson Packs, CSV, Story Quiz (импорт)

### 15.1 Lesson Pack (единица импорта)
Пакет содержит:
- `manifest.json`
- CSV по урокам
- (опционально) story quiz JSON файлы
- (опционально) vocabulary packs (vNext)

### 15.2 manifest.json (обязательные поля)
- `schemaVersion` (int)
- `packId` (string)
- `packVersion` (string)
- `language` (string)
- `lessons[]`: `lessonId`, `order`, `title?`, `file`

Пример:
```json
{
  "schemaVersion": 1,
  "packId": "EN_Core_A1",
  "packVersion": "v1",
  "language": "EN",
  "lessons": [
    { "lessonId": "L01_PRESENT_SIMPLE", "order": 1, "title": "Present Simple", "file": "lesson_01.csv" }
  ]
}
```

### 15.3 CSV урока (строго)
- UTF-8
- separator: `;`
- header всегда: `ru;en;alt_en;comment`

Семантика:
- `ru` — исходное русское предложение (без подсказок в скобках)
- `en` — основной эталонный перевод
- `alt_en` — допустимый альтернативный вариант (для сравнения; в UI не показывать)
- `comment` — служебное примечание

### 15.4 Story Quiz JSON (строго)
- `storyId`, `lessonId`, `phase` (CHECK_IN/CHECK_OUT)
- `text`
- `questions[]`: `qId`, `prompt`, `options[]`, `correctIndex`, `explain?`

Пример:
```json
{
  "storyId": "S_L01_OUT_01",
  "lessonId": "L01_PRESENT_SIMPLE",
  "phase": "CHECK_OUT",
  "text": "Tom works in a small shop...",
  "questions": [
    {
      "qId": "Q1",
      "prompt": "Where does Tom work?",
      "options": ["In a shop", "In a hospital", "At school", "At home"],
      "correctIndex": 0
    }
  ]
}
```

---

## 16) Доменная модель (обновление)

### 16.1 Основные сущности
- `Profile`
- `Settings` (версионируемые; допускают extras для расширений)
- `LessonPack`
- `Lesson` (12 тем)
- `SubLesson` (10–12 узлов на тему)
- `SentenceItem` (RU→Target)
- `StoryQuiz` (Check‑in/Check‑out)
- `BossChallenge` (Final/Super)
- `TrainingSession`
- `SkillMatrix` (12 плиток уроков)

### 16.2 Пример доменных типов (ориентир)
```kotlin
data class Lesson(
  val lessonId: String,
  val order: Int,
  val title: String?,
  val activeItems: List<SentenceItem>,
  val reserveItems: List<SentenceItem>,
  val nodes: List<SubLessonNode>,
  val storyCheckIn: List<StoryQuiz>,
  val storyCheckOut: List<StoryQuiz>
)

data class SubLessonNode(
  val nodeId: String,
  val order: Int,
  val kind: NodeKind // NEW_ONLY or MIXED (в будущем возможно больше)
)

enum class NodeKind { NEW_ONLY, MIXED }

data class SentenceItem(
  val itemId: String,
  val ru: String,
  val en: String,
  val altEn: String? // для проверки, не для UI
)

data class StoryQuiz(
  val storyId: String,
  val lessonId: String,
  val phase: StoryPhase,
  val text: String,
  val questions: List<StoryQuestion>
)

enum class StoryPhase { CHECK_IN, CHECK_OUT }

data class StoryQuestion(
  val qId: String,
  val prompt: String,
  val options: List<String>,
  val correctIndex: Int,
  val explain: String?
)
```

---

## 17) Метрики навыка и матрица закрепления (Chunk + Freshness)

### 17.1 Метрика A — ChunkStrength% (формирование автоматизма паттерна)
Смысл: “насколько физически собран паттерн” с учётом того, что без повторов прогресс частично “проседает”.

Что считается практикой по теме (`lesson_id`):
- каждая закрытая карточка темы (переход к следующей), независимо от STT,
- либо упрощённо: закрытие подурока добавляет N практик (N = число карточек подурока).

Модель (логарифмическая, учитывает всю историю + пропуски):
- агрегируем практику по календарным дням: `n_j` = сколько карточек темы было пройдено в день `j`
- `Δ_j` = сколько дней прошло с дня `j` до “сейчас”
- параметры: `d = 0.5`, `ε = 0.25 дня` (6 часов)

`B = ln( Σ ( n_j × (Δ_j + ε)^(-d) ) )`

Дальше нормируем к целевому “идеалу 150 повторений”:
- заранее считаем эталон `B_150`
- считаем прогресс:

`ChunkStrength% = clamp(100 × B / B_150, 0..100)`

Порог закрепления темы (дефолт):
- `ChunkStrength% ≥ 75%` → тема считается “закреплённой” (условно)
- `100%` → “очень сильная” фиксация

### 17.2 Метрика B — Freshness% (скорость/доступность навыка после закрепления)
Пока `ChunkStrength% < 75%`:
- Freshness не показывается.

Как только `ChunkStrength% ≥ 75%`:
- появляется FLOWER и начинает работать Freshness.

Touch по теме засчитывается, если за день пользователь:
- прошёл ≥ `K` карточек темы (дефолт `K=10`), **или**
- прошёл 1 подурок, где тема присутствовала (в том числе в Mixed/Elite).

`t` = дней с последнего touch.

Экспоненциальная модель:
`Freshness_raw = 100 × 2^(−t / H)`

`H` (half-life) дефолт: **30 дней**.

Порог заброшенности:
- `MaxNeglectDays = 90`
- если `t ≥ 90`: `Freshness% = 0`, цветок исчезает, появляется “ржавчина/паутинка” (как overlay).

### 17.3 Влияние Hint-rate на закрепление (добавление к модели)
Hint-rate — продуктовый gate поверх модели:

- даже если `ChunkStrength%` достигает порога, тема **не переходит в FLOWER**, если:
  - `hintUsageRate > 40%` (остаётся SPROUT)
  - при `hintUsageRate > 60%` — FLOWER недоступен, пока показатель не станет ниже (дальнейшая практика без подсказок)

---

## 18) Итог: что пользователь получает
- После 12 тем — процедурный каркас базовой грамматики.
- Boss-челленджи — стресс-тест устойчивости.
- Elite Mode — фоновая поддержка навыков без календарной дисциплины.
- Story Check‑in/out — измерение “до/после” по пониманию без тяжёлого ввода.
- Vocabulary Sprint — активное извлечение слов без смешивания в New-only.

---

## 19) Короткая “конституция” (1 абзац)
GrammarMate учит грамматику как навык через RU→Target retrieval: курс состоит из 12 тем, каждая тема = один грамматический паттерн и набор предложений, разделённый на Active и Reserve пул; каждая тема разбита на 10–12 подуроков по 8–15 фраз с обязательным Warm‑up на 3 лёгких предложения; в каждой теме первые 3–4 подурока — New-only (только новая тема), далее включается Mixed по отдельным правилам шедулера и лестнице интервальных повторов в шагах подуроков; подсказки разрешены, но высокий hintUsageRate блокирует признание автоматизма (переход в FLOWER), пока доля подсказок не снизится; вокруг каждой темы стоят Story Check‑in/Check‑out в формате истории + квиз на выбор ответов; после завершения темы доступны опциональные Boss‑челленджи, а после 12‑й темы включается Elite Mix — бесконечный цикл поддержания навыка без привязки к календарю.
